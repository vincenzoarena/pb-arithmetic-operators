/**
 * Q4. Calculate the perimeter of a square. Assume each side is 4.75cm.
 * 
 * `The perimeter of a square is the length of each side summed together.`
 * 
 */

let squareSide = 4.75

console.log((squareSide * 4) + "cm");

// ✅

/**
 * Q5. Calculate the perimeter of a triangle. Assume the length of the sides are 5cm, 6cm, 7cm.
 */

let a = 5;
let b = 6;
let c = 7;

console.log((a + b + c) + "cm");

// ✅

/**
 * Q6. Calculate the area of a square. Each side is 5cm.
 */

let newSquareSide = 5;

console.log((newSquareSide ** 2) + "cm2");

// ✅

/**
 * Q7. Calculate the area of an [right-angled triangle](https://en.wikipedia.org/wiki/Right_triangle).
 * Assume the length of the sides are 3cm, 4cm, 5cm.
 */

let base = 3;
let height = 4;

console.log(((base * height) / 2) + "cm2");

// ✅

/**
 * Q8. Calculate the volume of a cube. Length of each side is 9cm.
 */

let cubeSide = 9;

console.log((cubeSide ** 3) + "cm3");

// ✅

/**
 * Solutions:
 * 19cm
 * 18cm
 * 25cm2
 * 6cm2
 * 729cm3
 * 
 */